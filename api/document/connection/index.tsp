/**
 * Connection 模块 (Connection Module)
 *
 * 提供跨组织的数据分享和传播功能。
 * Provides cross-organization data sharing and propagation.
 *
 * 模块结构:
 * - models.tsp - Connection 数据模型
 * - api.tsp - Connection API 接口
 *
 * 核心功能:
 * - 跨组织的 Connection 创建和管理
 * - ConnectionBinding 关联和审批
 * - Inbound/Outbound 配置
 * - 数据同步和传播
 */
import "./models.tsp";
import "./api.tsp";
